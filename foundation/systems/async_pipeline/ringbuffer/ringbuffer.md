### 1. Ring Buffer (環形緩衝區)
Ring Buffer 是一種固定大小的緩衝區，其頭尾相連，常用於處理串流資料或非同步通訊（如 Producer-Consumer 模型）。

核心特性
固定空間：預先分配一塊連續記憶體，避免動態分配帶來的碎片化。

先進先出 (FIFO)：資料由寫入指標（Write/Head）進入，由讀取指標（Read/Tail）離開。

覆寫機制：當緩衝區滿了，新資料通常會覆寫舊資料，或停止寫入直到空間釋放。

適用場景
硬體驅動程式：如 UART 或 DMA 的資料接收。

音訊處理：需要穩定、連續的資料流。

日誌系統：保留最近發生的事件。